# CI workflows removed for Suricate SDK bootstrap.

Upstream OpenHands software-agent-sdk GitHub Actions were deleted so pushes
do not fail on pre-commit / TypeScript client / release pipelines that are
not configured for this fork.

Re-enable later under `.github/workflows/` when ready.
