[project]
name = "openhands-sdk"
version = "1.48.0"
description = "Suricate SDK - Core functionality for building AI agents"

requires-python = ">=3.12"
dependencies = [
    "agent-client-protocol>=0.10.1,<0.11.0",  # 0.11.0 reordered prompt() args, broke the client (#4830)
    "deprecation>=2.1.0",
    "fakeredis[lua]>=2.32.1",  # Explicit dependency for docket/fastmcp background tasks
    "fastmcp>=3.2.0",  # 3.2.0 reads back the stored absolute OAuth token expiry
    "filelock>=3.20.1",
    "httpx[socks]>=0.27.0",
    "jsonschema>=4.23.0",
    "joserfc>=1.7.5",
    "litellm>=1.93.0",
    "pillow>=12.3.0",
    "pydantic>=2.12.5",
    "python-frontmatter>=1.1.0",
    "python-json-logger>=3.3.0",
    "tenacity>=9.1.2",
    "websockets>=12",
    "lmnr>=0.7.60,<0.8.0",
    "tree-sitter>=0.25",
    "tree-sitter-bash>=0.25",
]

[project.urls]
Source = "https://github.com/yqwd-dimleap/suricate-sdk"
Homepage = "https://github.com/yqwd-dimleap/suricate-sdk"
Documentation = "https://docs.openhands.dev/sdk"
"Bug Tracker" = "https://github.com/yqwd-dimleap/suricate-sdk/issues"

[project.optional-dependencies]
boto3 = ["boto3>=1.35.0"]
toolshield = ["toolshield>=0.1.3,<0.2"]
vertex = ["google-cloud-aiplatform>=1.38"]

[build-system]
requires = ["setuptools>=83.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.package-dir]
"" = "."

[tool.setuptools.packages.find]
include = ["openhands.sdk*"]
namespaces = true

[tool.setuptools.package-data]
"*" = ["py.typed", "*.j2", "schemas/*.json"]
